[package]
name = "eltanin-protocol"
version = "0.1.0"
edition = "2021"
publish = false
description = "Eltanin protocol crate (MVP 1.0 scaffold - populated by its owning Feature ticket)."

[lints]
workspace = true

[dependencies]
