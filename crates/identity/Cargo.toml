[package]
name = "eltanin-identity"
version = "0.1.0"
edition = "2021"
publish = false
description = "Eltanin identity crate (MVP 1.0 scaffold - populated by its owning Feature ticket)."

[lints]
workspace = true

[dependencies]
